flask>=3.0
flask-login>=0.6
bcrypt>=4.0
reportlab>=4.0
openpyxl>=3.1
waitress>=3.0

# PostgreSQL stack (SPEC-002a — infrastructure only, not wired into app.py yet).
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
